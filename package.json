{
  "name": "mfl-draft-dashboard",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "pnpm --parallel --filter @mfl/api --filter @mfl/web dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @mfl/api start"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
